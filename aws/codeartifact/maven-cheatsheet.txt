# to run intergeration tests
mvn -s ~/.m2/settings.xml -pl integration -am clean verify -Dskip.docker=false


# to test variables
mvn -s ~/.m2/settings.xml help:evaluate -Dexpression=docker.postgres -q -DforceStdout
mvn -s ~/.m2/settings.xml help:evaluate -Dexpression=docker.jboss-pg -q -DforceStdout
