apiVersion: cert-manager.io/v1
kind: ClusterIssuer # I'm using ClusterIssuer here
metadata:
  name: letsencrypt-prod-mi
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: madhav.kundala@usda.gov
    privateKeySecretRef:
      name: letsencrypt-prod-mi
    solvers:
    - http01:
        ingress:
          class: nginx 
