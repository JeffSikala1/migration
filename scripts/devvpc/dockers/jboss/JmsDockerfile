FROM 339713019047.dkr.ecr.us-east-1.amazonaws.com/conexus-jboss:8.0.3.1

ARG TASKEAR
USER jboss

ARG EAP_DEPLOY_DIR=/app
ARG EAP_VERSION=8.0
ENV JBOSS_HOME="${EAP_DEPLOY_DIR}/jboss-eap-${EAP_VERSION}"

COPY ${TASKEAR}  ${JBOSS_HOME}/standalone/deployments/task-ear.ear

CMD ["sh", "-c", "${JBOSS_HOME}/bin/standalone.sh -c=standalone-full.xml"]