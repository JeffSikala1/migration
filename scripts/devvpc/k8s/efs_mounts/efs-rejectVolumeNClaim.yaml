kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: efs-sc-reject
provisioner: efs.csi.aws.com
parameters:
  provisioningMode: efs-ap
  fileSystemId: ${fs_reject}
  directoryPerms: "755"
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: efs-reject-volume
spec:
  capacity:
    storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: efs-sc-reject
  csi:
    driver: efs.csi.aws.com
    volumeHandle: ${fs_reject}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: efs-reject-volume-claim
  namespace: ingress-nginx
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc-reject
  resources:
    requests:
      storage: 5Gi
