apiVersion: v1
kind: Service
metadata:
  name: conexus-httpd
  namespace: ingress-nginx
  labels:
    app: conexus-httpd
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  
  selector:
    app.kubernetes.io/name: conexus-httpd

